This directory contains a small subset of the X11R4 header files -- all
that is needed to build pigiRpc on systems that are missing them for some
reason (you'll also need the X libraries, of course).
