% Style option for Ptolemy documentation.
% Version $Id$
% Author:	T. M. Parks
% Created:	23 November 1993

%
% Use LaTeXinfo as the base style.
%
\input{latexinfo.sty}

%
% Commands for listing authors and contributors of a chapter.
%
% Names should be listed one per line, separated by \\.
%
\def\Author#1{
    \noindent
    \makebox[1.5in][l]{\em Authors:}
    \parbox[t]{4in}{\em #1} \par
    \vspace{\baselineskip} }
\def\Contrib#1{
    \noindent
    \makebox[1.5in][l]{\em Other Contributors:}
    \parbox[t]{4in}{\em #1} \par
    \vspace{\baselineskip} }

%
% A list environment for blocks (stars, galaxies, universes).
% The parameter specifies a suffix to be added after item names
% for index entries.
%
% The \block command generates a list item.
% The first argument is the name of the block.
%
\newenvironment{blocklist}[1]%
    {\begin{description}\def\@indexsuffix{#1}}%
    {\end{description}}
\newcommand{\block}[1]{\item[\code{#1}]\tpindexbold{#1 \@indexsuffix}}

%
% A list environment for states (or parameters).
% The parameter specifies a suffix to be added after item names
% for index entries.
%
% The \state command generates a list item.  The first argument is the
% data type.  The second argument is the name of the state.  The third
% argument is the default value.
%
\newenvironment{statelist}[1]%
    {\begin{description}\def\@indexsuffix{#1}}%
    {\end{description}}

\newcommand{\state}[3]{\item[\code{#2}](\code{#1}) Default = \samp{#3}\\
    \vrindexbold{#2 \@indexsuffix}}

%
% A list environment for ports.
% The parameter specifies a suffix to be added after item names
% for index entries.
%
% The \port command generates a list item.  The first argument is the
% data type.  The second argument is the name of the port.
%
\newenvironment{portlist}[1]%
    {\begin{description}\def\@indexsuffix{#1}}%
    {\end{description}}
\newcommand{\port}[2]{\item[\code{#2}](\code{#1})\\
    \vrindexbold{#2 \@indexsuffix}}

